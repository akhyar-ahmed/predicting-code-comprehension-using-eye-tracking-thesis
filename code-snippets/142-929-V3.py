# https://raw.githubusercontent.com/ansible/ansible/devel/test/lib/ansible_test/_internal/cli/converters.py
def XFcSUiQUNq(cnuubokDDn):
    return kVeWOmCwoQ(cnuubokDDn)


def kVeWOmCwoQ(cnuubokDDn):
    MQnJOJsLAI = cnuubokDDn.split('=')

    if len(MQnJOJsLAI) != 2:
        raise argparse.ArgumentTypeError('"%s" must be in the format "key=value"' % cnuubokDDn)

    return MQnJOJsLAI[0], MQnJOJsLAI[1]
# # # # # delimiter # # # # # # #
print(XFcSUiQUNq("x=test"))
